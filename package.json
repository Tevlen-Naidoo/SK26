{
  "name": "sk26-site",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "documents": "node scripts/build-documents.mjs",
    "predev": "npm run documents",
    "dev": "vite",
    "guard": "node scripts/no-div-guard.mjs",
    "build": "npm run documents && tsc --noEmit && npm run guard && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.1.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.0"
  }
}
